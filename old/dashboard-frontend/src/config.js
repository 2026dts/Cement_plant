// Point these at your backend (Architecture v5, Section 4). During local dev
// the backend usually runs on the same machine at port 4000.
export const API_BASE = "http://localhost:4000";
export const WS_URL = "ws://localhost:4000/ws";
